# Agent 入口规则

任何开发 Agent、自动化工具或人工开发者在读取、修改、测试本项目之前，必须：

1. 完整读取根目录 `limitation.md`。
2. 按其中的“规则优先级”和“完成定义”执行。
3. 只按需读取 `limitation.md` 索引的详细文档。
4. 新增 MCP、数据源、外部写操作或敏感数据前，先更新 `limitation.md` 并取得用户确认。

若实现与 `limitation.md` 冲突，停止扩展并明确报告冲突，不得用临时代码绕过。

